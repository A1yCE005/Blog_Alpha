import type { CSSProperties } from "react";

export const viewportMinHeightStyle: CSSProperties = {
  minHeight: "var(--viewport-height)",
};

export const viewportHeightStyle: CSSProperties = {
  height: "var(--viewport-height)",
  maxHeight: "var(--viewport-height)",
  minHeight: "var(--viewport-height)",
};
